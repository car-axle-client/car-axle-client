import { moduleDefinition } from '../../moduleapi'

const plugin: moduleDefinition = {
    display_name: 'selenite',
    description: 'for their cloaks list that I blatantly stole and most of the games',
    id: 'cred',
    section: 'credit',
    disabled: true,
    custom_render: false,
}

export default plugin
